// @flow

export type Props = { [key: string]: any };

export type Element = {
  x: number,
  y: number,
  parentsOffsetX: number,
  parentsOffsetY: number,
  text: string,
};

export type Position = {
  x: number,
  y: number,
};
